# Galaxy Local Engine 0.11.0

这一版继续把 Local Engine 从下载弹窗升级为本机下载工作台，重点补齐任务历史、任务详情和等待队列控制。

## 下载历史

- 新增本机持久化下载历史，最多保留最近 80 条终态任务。
- 记录完成 / 失败 / 取消状态、完成时间、来源站点、目标画质、文件名与耗时。
- 历史仅保存在安装目录的 `state/download-history.json`。
- 来源 URL 自动移除查询参数、片段和 URL 凭据，避免把 token/cookie 类敏感参数写入历史。
- 历史窗口支持打开文件、定位文件、复制脱敏后的来源地址和一键清空历史。
- 清空历史不会删除已经下载的媒体文件。

## 当前任务详情

- 新增“任务详情”窗口，直接显示最终交给本机下载器的关键参数：视频/音频质量、字幕来源与语言、音轨语言、片段范围、章节拆分、SponsorBlock、aria2c 和集合模式。
- 新增“打开文件”按钮，完成下载后可以直接打开最终媒体文件。

## 队列控制

- 等待任务新增上移 / 下移，可在不影响当前任务的前提下调整后续执行顺序。
- 新增“完成后暂停”队列开关：当前任务继续正常下载，完成后不自动启动下一项。
- 队列暂停时可以继续接收等待任务；点击“继续队列”后恢复执行。
- 现有单项移除、清空等待队列、容量限制和 FIFO 默认行为继续保留。

## 回归边界

- 不修改 0.9/0.10 已验证的媒体解析优先级、腾讯元宝浏览器登录复用、yt-dlp / FFmpeg 下载主路径、Archive、图片下载和单实例 Bridge 语义。
- SponsorBlock、aria2c、章节拆分、片段下载等高级项继续默认关闭。
- 网站下载地址固定绑定 `local-engine-v0.11.0`，不使用 `releases/latest`。
