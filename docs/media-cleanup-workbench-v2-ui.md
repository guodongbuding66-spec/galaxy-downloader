# Workbench 2.0 UI

The desktop workbench keeps the existing file picker, manual region selection, automatic suggestion review, cancellation, and output-folder controls. It adds a processing-mode selector for video and a Before/After comparison action after successful cleanup.
