# Workbench 2.0 safety boundary

The workbench operates only on user-selected, visible pixels in local media files.

It does not inspect, target, or remove invisible provenance/authenticity systems such as C2PA or SynthID. Automatic suggestions remain visual candidates and are never applied without user confirmation. Outputs are non-destructive and use separate files.
