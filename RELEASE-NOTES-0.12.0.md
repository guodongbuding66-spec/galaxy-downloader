# Galaxy Local Engine 0.12.0

这一版继续把 Local Engine 从“可用的下载工作台”推进到更完整的本机下载管理器，重点补齐历史重试、批量队列管理、工作台设置和输出文件组织。

## 下载历史升级

- 历史窗口新增搜索与完成 / 失败 / 取消状态筛选。
- 新增“重新下载”：会按历史任务保存的画质、字幕、音轨、章节、SponsorBlock、aria2c 等参数重新提交任务。
- 用户主动点击重试时会临时关闭 Download Archive 跳过逻辑，避免“重试”被 Archive 直接忽略。
- 历史重试继续执行隐私保护：未知查询参数、URL 凭据、fragment 不会写入磁盘。
- YouTube `v` / `list` / `index` 这类稳定媒体标识会保留，因此常见 YouTube 历史任务可以正常重试；跟踪参数仍会删除。
- 历史保留数量可以设置为 20 / 50 / 80 / 150 / 300 条，也可以完全关闭新增历史记录。

## 队列管理器

- 新增独立“队列管理”窗口，可查看全部等待任务而不局限于主界面的前 8 项。
- 支持多选等待任务批量移除。
- 支持多选任务整体移到顶部或底部，并保持所选任务原有相对顺序。
- 支持上移 / 下移、清空全部、暂停 / 继续队列。
- 所有队列管理操作只作用于等待列表，不会中断当前正在下载的任务。

## 工作台设置

- 新增独立设置中心。
- 文件命名支持三种安全预设：`标题 [ID]`、`仅标题`、`ID - 标题`。
- 默认仍保持现有的 `标题 [ID]`，升级后不会改变既有下载行为。
- 可选“按来源解析器建立子目录”，例如将 YouTube / Bilibili 的新任务分别整理到对应子目录。
- 设置保存在本机 `state/workspace-options.json`，不上传到服务器。

## 当前任务详情

- 任务详情继续显示下载参数，并新增 Download Archive、文件命名方式、按来源整理状态和最终 yt-dlp 输出模板。

## 稳定性边界

- 不修改 0.9–0.11 已验证的媒体优先解析、腾讯元宝浏览器登录复用、yt-dlp / FFmpeg 主下载路径、图片下载、文档解析、单实例 Bridge 和安全退出语义。
- 所有新增输出设置都有与旧版本完全一致的默认值。
- 网站安装包继续固定绑定 `local-engine-v0.12.0`，不使用 `releases/latest`。
