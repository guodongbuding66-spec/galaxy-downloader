# Public HTTPS hostname used by Caddy, without scheme.
XHS_RESOLVER_DOMAIN=xhs-resolver.example.com

# Use a long random value. Galaxy sends this as Authorization: Bearer <token>.
XHS_RESOLVER_TOKEN=replace-with-a-long-random-token

# Optional ACME account email for Caddy certificate notifications.
ACME_EMAIL=admin@example.com
