name: Telegram Transfer

on:
  pull_request:
    paths:
      - "local-engine/telegram_transfer.py"
      - "local-engine/media_library.py"
      - "local-engine/runtime_storage.py"
      - "scripts/test-local-telegram-transfer.py"
      - ".github/workflows/telegram-transfer.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  telegram-transfer:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install Local Engine dependencies
        run: python -m pip install -r local-engine/requirements.txt
      - name: Compile Telegram transfer path
        run: >-
          python -m py_compile
          local-engine/telegram_transfer.py
          local-engine/media_library.py
          local-engine/runtime_storage.py
          scripts/test-local-telegram-transfer.py
      - name: Test Telegram transfer contract
        run: python scripts/test-local-telegram-transfer.py
