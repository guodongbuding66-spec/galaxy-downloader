name: Bootstrap History Sync Hook Convergence

on:
  push:
    branches:
      - refactor/local-engine-history-sync-hooks
    paths:
      - '.github/workflows/bootstrap-history-sync-hooks.yml'

permissions:
  contents: write

jobs:
  converge:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          ref: refactor/local-engine-history-sync-hooks

      - name: Apply history sync transform
        run: python scripts/refactor-history-sync-hooks.py

      - name: Compile desktop modules
        run: |
          python -m py_compile \
            local-engine/desktop_hooks.py \
            local-engine/desktop_extras.py \
            local-engine/task_center.py

      - name: Run deterministic contracts
        run: |
          python scripts/test-local-desktop-hooks.py
          python scripts/test-local-manager-policy.py
          python scripts/test-local-runtime-policy.py
          python scripts/test-local-recovery-policy.py
          python scripts/test-local-pause-resume.py

      - name: Commit history sync convergence
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add local-engine/desktop_hooks.py local-engine/desktop_extras.py local-engine/task_center.py scripts/test-local-desktop-hooks.py
          git commit -m 'refactor(local-engine): replace history sync wrapper with hook'
          git push origin HEAD:refactor/local-engine-history-sync-hooks
