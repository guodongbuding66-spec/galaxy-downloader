name: Learning Progress Sync

on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'local-engine/web-dashboard/learning-player.js'
      - 'local-engine/test_headless_learning_progress_ui.py'
      - '.github/workflows/learning-progress-sync.yml'
  push:
    branches:
      - main
    paths:
      - 'local-engine/web-dashboard/learning-player.js'
      - 'local-engine/test_headless_learning_progress_ui.py'
      - '.github/workflows/learning-progress-sync.yml'

permissions:
  contents: read

jobs:
  contract:
    name: ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    timeout-minutes: 8
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest
          - windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4

      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: '3.12'

      - name: Compile progress contract
        run: python -m py_compile local-engine/test_headless_learning_progress_ui.py

      - name: Validate player JavaScript syntax
        run: node --check local-engine/web-dashboard/learning-player.js

      - name: Run progress sync contract
        run: python local-engine/test_headless_learning_progress_ui.py
