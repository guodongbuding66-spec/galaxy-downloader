name: Reader PDF Core

on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'local-engine/requirements.txt'
      - 'local-engine/reader_pdf.py'
      - 'local-engine/reader_workspace.py'
      - 'scripts/test-local-reader-pdf.py'
      - '.github/workflows/reader-pdf-core.yml'
  push:
    branches: [main]
    paths:
      - 'local-engine/requirements.txt'
      - 'local-engine/reader_pdf.py'
      - 'local-engine/reader_workspace.py'
      - 'scripts/test-local-reader-pdf.py'
      - '.github/workflows/reader-pdf-core.yml'

permissions:
  contents: read

jobs:
  pdf-core:
    name: ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    timeout-minutes: 12
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
        with:
          python-version: '3.12'
      - run: python -m pip install -r local-engine/requirements.txt
      - run: python -m py_compile local-engine/reader_pdf.py scripts/test-local-reader-pdf.py
      - run: python scripts/test-local-reader-pdf.py
