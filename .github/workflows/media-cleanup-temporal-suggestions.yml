name: Media Cleanup Temporal Suggestions

on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'local-engine/media_cleanup.py'
      - 'local-engine/media_cleanup_suggestions.py'
      - 'scripts/test-local-media-cleanup-suggestions.py'
      - '.github/workflows/media-cleanup-temporal-suggestions.yml'
  push:
    branches:
      - main
    paths:
      - 'local-engine/media_cleanup.py'
      - 'local-engine/media_cleanup_suggestions.py'
      - 'scripts/test-local-media-cleanup-suggestions.py'
      - '.github/workflows/media-cleanup-temporal-suggestions.yml'

permissions:
  contents: read

jobs:
  contract:
    name: ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest
          - windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: '3.12'

      - name: Compile contract
        run: python -m py_compile local-engine/media_cleanup.py local-engine/media_cleanup_suggestions.py scripts/test-local-media-cleanup-suggestions.py

      - name: Run temporal suggestion tests
        run: python scripts/test-local-media-cleanup-suggestions.py
