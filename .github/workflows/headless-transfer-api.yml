name: Headless Transfer API

on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'local-engine/headless_transfer_api.py'
      - 'local-engine/transfer_center.py'
      - 'local-engine/media_library.py'
      - 'scripts/test-local-headless-transfer-api.py'
      - '.github/workflows/headless-transfer-api.yml'
  push:
    branches:
      - main
    paths:
      - 'local-engine/headless_transfer_api.py'
      - 'local-engine/transfer_center.py'
      - 'local-engine/media_library.py'
      - 'scripts/test-local-headless-transfer-api.py'
      - '.github/workflows/headless-transfer-api.yml'

permissions:
  contents: read

jobs:
  contract:
    name: ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest
          - windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4

      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: '3.12'

      - name: Install local engine requirements
        run: python -m pip install -r local-engine/requirements.txt

      - name: Compile Headless Transfer contract
        run: python -m py_compile local-engine/headless_transfer_api.py scripts/test-local-headless-transfer-api.py

      - name: Run Headless Transfer self-test
        run: python scripts/test-local-headless-transfer-api.py
