name: Bootstrap Pause Resume Core

on:
  push:
    branches:
      - feat/local-engine-0.15-pause-resume
    paths:
      - '.github/workflows/bootstrap-pause-resume-core.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          ref: feat/local-engine-0.15-pause-resume

      - name: Apply web bridge transforms
        run: python scripts/bootstrap_pause_resume_web_bridge.py

      - name: Compile Python bridge
        run: python -m py_compile local-engine/resume_bridge.py

      - name: Set up pnpm and Node.js
        uses: pnpm/setup@703c52620218391530e48b9e8870d5c0082e1b9b # v2.1.0
        with:
          version: 11.8.0
          runtime: node@22
          cache: true
          install: false

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run Local Engine bridge tests
        run: pnpm exec vitest run tests/local-engine-bridge-error.test.ts

      - name: Lint changed TypeScript
        run: pnpm exec eslint src/lib/local-engine-bridge.ts tests/local-engine-bridge-error.test.ts

      - name: Commit integration
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add local-engine/resume_bridge.py src/lib/local-engine-bridge.ts tests/local-engine-bridge-error.test.ts
          git commit -m 'feat(local-engine): add web pause resume bridge controls'
          git push origin HEAD:feat/local-engine-0.15-pause-resume
